#include <stdio.h>

int main() {
    int a = 5;
    int b = 10;
    int risultato = 0; 

    risultato = risultato + a + b; 

    printf("Risultato finale: %d + %d = %d\n", a, b, risultato);

    return 0;
}
